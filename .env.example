# OpenAI (required for content generation)
OPENAI_API_KEY=

# Redis URL for BullMQ (required for queue + worker), e.g. redis://default:pass@host:6379
REDIS_URL=

# Ayrshare — omit for local dev (distribution logs only, no live post)
AYRSHARE_API_KEY=
